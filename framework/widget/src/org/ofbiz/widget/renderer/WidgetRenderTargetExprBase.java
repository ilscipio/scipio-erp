package org.ofbiz.widget.renderer;

import org.ofbiz.webapp.renderer.RenderTargetExprBase;

/**
 * SCIPIO: DEV NOTE: this is kind of a backward adaptation, if I had to rewrite all the classes
 * I would organize them differently...
 */
@SuppressWarnings("serial")
public abstract class WidgetRenderTargetExprBase implements RenderTargetExprBase {
    protected WidgetRenderTargetExprBase() {
    }
}
