package com.ilscipio.scipio.order.payment.codeplugin;

/**
 * Base payment method code plugin handler.
 * <p>
 * Each component should have its own sub-interface to define the methods it needs
 * (due to build dependencies).
 */
public interface PayMethPluginHandler {

}
